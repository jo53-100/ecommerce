asgiref==3.11.1
Django==6.0.6
# Reads BASE_DIR/.env into os.environ at startup (see settings.py).
python-dotenv==1.2.2
pillow==12.3.0
sqlparse==0.5.5

# --- Production serving (used by the VPS deploy) ---
gunicorn==23.0.0
whitenoise==6.9.0

# --- Payments ---
stripe==15.3.1

# --- Back office ---
# Only needed by `manage.py import_excel`, which seeds the dashboard from the
# historical NOMINA / BASE_DE_DATOS spreadsheets.
openpyxl==3.1.5
